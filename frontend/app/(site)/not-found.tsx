import NotFoundContent from "@/components/site/NotFoundContent";

/** Trang trong nhóm gọi `notFound()`, ví dụ mở một mã đơn không có thật. */
export default function NotFound() {
  return <NotFoundContent />;
}
